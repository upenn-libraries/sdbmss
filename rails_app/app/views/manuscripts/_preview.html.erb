<% manuscript = record %>
<div class="card-header">
    <strong class="h4"><%= link_to manuscript.public_id, manuscript_path(manuscript) %></strong>
    <% if !manuscript.reviewed %>
          <span class="text-warning"><span class="fa fa-exclamation-triangle"></span> Unapproved</span>
    <% end %>
</div>
<div class="card-body">
<dl class="document-metadata">
  <dt>Title(s)</dt>
  <dd>
    <%= manuscript.all_titles.to_a.join(", ") %>
  </dd>
  <dt># Entries</dt>
  <dd>This Manuscript is linked to <%= manuscript.entries.count %> entries.</dd>
</dl>
<div class="text-center text-muted">
  <span class="fa fa-clock-o"></span> Added On <%= manuscript.created_at.to_formatted_s(:long) %>
</div>
</div>