<% content_for :breadcrumbs do %>
  <%= render partial: "dericci_games/breadcrumbs" %>
<% end %>
<div class="row">
  <div class="col-sm-6">
    <h3>Archive of Miscellaneous Notes</h3>
  </div>
  <div class="col-sm-6 text-right">
    <ul class="pagination justify-content-end">
      <% (1 + DericciNote.count / 20).times do |i| %>
        <li class="page-item <%= @page == i ? 'active' : '' %>"><%= link_to (i + 1), dericci_notes_path(page: i), class: "page-link" %></li>
      <% end %>
    </ul>
  </div>
  <div class="col-sm-12">
    <table class="table table-bordered w-100">
      <tbody>
        <% @notes.each do |note| %>
          <tr>
            <td><%= link_to note.name, note.link %></td>
            <td><%= note.cards %></td>
            <td><%= note.size %></td>
            <td class='text-right'><%= note.senate_house %></td>
          </tr>
        <% end %>
      </tbody>
    </table>
  </div>
</div>
