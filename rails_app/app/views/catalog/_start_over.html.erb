<%#
  OVERRIDE Blacklight 7.11: appends search_field=all_fields so simple search defaults to the app's
    all-fields search; stock partial uses start_over_path without extra params.
%>
<%= link_to t('blacklight.search.start_over'), start_over_path + "?search_field=all_fields&utf8=%E2%9C%93", class: "catalog_startOverLink btn btn-primary float-right" %>
