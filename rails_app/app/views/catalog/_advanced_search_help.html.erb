<% if params[:f] and params[:f].keys.count > 0 %>
  <div class="facets accordion">
    <div class="accordion-item facet-limit facet-limit-active">
      <h3 class="accordion-header p-0 facet-field-heading h6" id="adv-selected-facets-header">
        <button type="button"
          class="btn accordion-button"
          data-bs-toggle="collapse"
          data-bs-target="#adv-selected-facets"
          aria-expanded="true" aria-controls="adv-selected-facets">
          Selected Filters
        </button>
      </h3>
      <div id="adv-selected-facets" role="region" aria-labelledby="adv-selected-facets-header"
        class="panel-collapse facet-content collapse show">
        <div class="accordion-body">
          <ul class="list-unstyled facet-values">
            <%= render_constraints_filters_side(params) %>
          </ul>
        </div>
      </div>
    </div>
  </div>
<% end %>
<div class='card'>
  <div class="card-header">Search tips</div>
  <div class="card-body">
    <ul class="advanced-help">
     <li>Truncation and wildcards are not supported - word-stemming is done automatically.</li>
     <li>Select "match all" to require all fields.
     </li>

     <li>Select "match any" to find at least one field.
     </li>

     <li>Combine keywords and attributes to find specific items.
     </li>

     <li>Use quotation marks to search as a phrase.

     <li>Use "+" before a term to make it required. (Otherwise results matching only some of your terms may be included).</li>

     <li>Use "-" before a word or phrase to exclude.

     <li>Use "OR", "AND", and "NOT" to create complex boolean logic. You can use parentheses in your complex expressions. </li>
   </ul>
  </div>
</div>
