<%= link_to(watches_path(watched: [id: watchable.id, type: watchable.class.to_s]), :method => :post, :remote => true, class: "watch Watch_#{watchable.class.to_s}_#{watchable.id}") do %>
    <span class="fas fa-fw fa-eye"></span> <span class="watch-text">Watch</span>
<% end %>