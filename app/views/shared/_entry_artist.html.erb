<% if entry_artist.artist %><%= link_to entry_artist.artist.name, name_path(entry_artist.artist) -%><% if entry_artist.observed_name.present? %> (recorded as <%= entry_artist.observed_name %>)<%- end -%><%- else -%><%= entry_artist.observed_name %><%- end -%><%= entry_artist.certainty_flags %>
