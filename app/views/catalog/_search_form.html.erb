<div class="navbar-header">
  <%= form_tag sdbmss_search_action_path, :method => :get, :class => 'form-inline clearfix navbar-form' do %>
    <%= render_hash_as_hidden_fields(params_for_search().except(:q, :search_field, :qt, :page, :utf8)) %>

    <% unless search_fields.empty? %>
      <!--<label for="q" style="margin-right: 10px;"><%= t('blacklight.search.form.search_field.label') %> </label>-->
      <% if false %>
      <%= select_tag(:search_field, options_for_select(search_fields, h(params[:search_field])), :title => t('blacklight.search.form.search_field.title'), :class=>"search_field form-control") %>
      <span class="sr-only"><%= t('blacklight.search.form.search_field.post_label') %></span>
      <% end %>
      <input type="hidden" name="search_field" value="all_fields"/>
    <% end %>
    <div class="input-group">
        <!--<label for="q" class="sr-only"><%= t('blacklight.search.form.q') %></label>-->
         <%= text_field_tag :q, params[:q], :placeholder => 'Search database...', :class => "search_q q form-control", :id => "q", :autofocus => should_autofocus_on_search_box? %>

      <span class="input-group-btn">
        <button type="submit" class="btn btn-primary search-btn" id="search">
          <span class="submit-search-text"><%=t('blacklight.search.form.submit')%></span>
          <span class="glyphicon glyphicon-search"></span>
        </button>
        <a type="button" href="<%= advanced_search_path(params_for_search) %>" class="btn btn-default advanced_search">Advanced</a>
        <button class='navbar-toggle btn btn-default' type="button" class="navbar-toggle" data-toggle="collapse" data-target="#myNavbar">
          <span class="glyphicon glyphicon-option-horizontal"></span>                       
        </button>
      </span>
  </div>
  <% end %>
</div>  
<div class="collapse navbar-collapse" id="myNavbar">
  <ul class="nav navbar-nav navbar-right">
    <% if user_signed_in? %>
      <li class="dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-expanded="false">My Searches <span class="caret"></span></a>
          <ul class="dropdown-menu" role="menu">
              <%= render_nav_actions do |config, action| %>
              <li><%= action %></li>
              <% end %>
          </ul>
      </li>
    <% end %>
    <li>
      <a style="" href="<%= recent_additions_path %>" class="recent_additions">Recent Additions</a>
    </li>
    <li>
      <a style="color: red;" href="<%= feedback_path %>" target='_blank'>Submit Feedback</a>
    </li>
  </ul>
</div>



