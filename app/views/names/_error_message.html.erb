<%= message[:message] %>
<% if (message[:message].include?("Name is already used") || message[:message].include?("Viaf ID is already used")) && !message[:message].include?("deleted") && can?(:merge, @model) %>
  <%= link_to "Merge #{@model.class.name}?", merge_name_path(@model, :action => "merge", :target_id => message[:name][:id]) %>
<% end %>