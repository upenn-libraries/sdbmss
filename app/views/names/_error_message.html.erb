<%= message %>
<% if message.include? "Name is already used" %>
    <a href="<%= merge_name_path(@model, target_id: 38461) %>">Click here to merge <%= @model.public_id %> into SDBM_NAME_<%= message.match(/\#(\d+)/)[1] %> instead.</a>
<% end %>
