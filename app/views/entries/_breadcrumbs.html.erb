<nav data-spy="affix" data-offset-top="102" id="breadcrumbs">
  <div class="panel panel-default">
    <div class="panel-body">
      <h1>Add a new Entry</h1>
    </div>
    <div class="panel-body">
      <ul class="nav nav-pills nav-stacked">
        <li class="<%= source ? 'active' : 'disabled'%>">
          <a href="#identify_source">Identify Source
          </a>
        </li>
        <li class="<%= !source ? 'active' : 'disabled'%>">
          <a href="#transaction_information">Transaction Information
          </a>
        </li>
        <li class="<%= !source ? '' : 'disabled'%>">
          <a href="#manuscript_details">Manuscript Details
          </a>
        </li>
        <li class="<%= !source ? '' : 'disabled'%>">
          <a href="#provenance_information">Provenance Information
          </a>
        </li>
      </ul>
    </div>
    <div class="panel-body text-center">
      <button class="btn btn-primary save-button" ng-click="save()" ng-disabled="entryForm.$invalid || currentlySaving">Save</button>
      <a class="btn btn-warning" id='cancelEdit' href="/dashboard">Cancel</a>
    </div>
  </div>
</nav>